        <div class="max-w-7xl mx-auto w-full mt-12">
            <div class="bg-white rounded-lg shadow-md p-6">
                <h3 class="text-lg font-semibold text-gray-800 mb-4 flex items-center">
                    <i class="fas fa-user-circle mr-2 text-indigo-600"></i>
                    Informations du compte
                </h3>

                <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
                    <div class="border-b border-gray-100 pb-2">
                        <span class="text-gray-500 text-sm">Nom</span>
                        <p class="font-medium">@Model.LastName @Model.FirstName</p>
                    </div>

                    <div class="border-b border-gray-100 pb-2">
                        <span class="text-gray-500 text-sm">Email</span>
                        <p class="font-medium">@Model.Email</p>
                    </div>

                    <div class="border-b border-gray-100 pb-2">
                        <span class="text-gray-500 text-sm">Membre depuis</span>
                        <p class="font-medium">@Model.RegisterDate.ToString("dd/MM/yyyy")</p>
                    </div>

                    <div class="border-b border-gray-100 pb-2">
                        <span class="text-gray-500 text-sm">Nombre de chiens</span>
                        <p class="font-medium">@Model.Dogs.Count</p>
                    </div>
                </div>

                <div class="mt-6">
                    <a href="#" class="text-indigo-600 hover:text-indigo-800 font-medium">
                        <i class="fas fa-cog mr-1"></i>
                        Modifier mes informations
                    </a>
                </div>
            </div>
        </div>